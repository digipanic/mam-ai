import { createFileRoute } from "@tanstack/react-router"; import { useState } from "react"; import { Sparkline } from "@/components/audience/charts"; import { PageFrame } from "@/components/audience/page-frame"; import { useAudienceMonitor } from "@/hooks/use-audience-monitor"; import { formatNumber, formatPercent, formatSigned } from "@/lib/audience-selectors";
export const Route=createFileRoute("/_dashboard/monthly-history")({head:()=>({meta:[{title:"Monthly History | Minor AM Audience Intelligence"},{name:"description",content:"Recorded monthly Instagram follower history across the roster."},{property:"og:title",content:"Monthly History | Minor AM Audience Intelligence"},{property:"og:description",content:"Recorded monthly Instagram follower history across the roster."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary"}]}),component:MonthlyHistory});
function MonthlyHistory(){const {data}=useAudienceMonitor();const [mode,setMode]=useState<'followers'|'change'|'growth'>('followers');const [search,setSearch]=useState('');if(!data)return null;const display=(a:any,index:number)=>{const now=a.monthlyHistory[index]?.followers??null,prev=a.monthlyHistory[index-1]?.followers??null;if(mode==='followers')return formatNumber(now);const c=now!==null&&prev!==null?now-prev:null;return mode==='change'?formatSigned(c):formatPercent(c!==null&&prev?c/prev*100:null)};return <PageFrame eyebrow="Monthly history" title="Recorded Instagram history." description="September is in progress. Gaps remain gaps; no values are interpolated."><div className="mb-7 flex flex-wrap justify-between gap-3"><input aria-label="Search monthly history" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search artists" className="h-9 border border-input bg-background px-3 text-sm"/><div className="flex border border-border text-xs">{(['followers','change','growth'] as const).map(m=><button key={m} onClick={()=>setMode(m)} className={`px-3 py-2 capitalize ${mode===m?'bg-secondary':''}`}>{m}</button>)}</div></div><div className="overflow-auto border-y border-border"><table className="min-w-[900px] w-full text-sm"><thead className="text-left text-[10px] uppercase tracking-[.1em] text-muted-foreground"><tr><th className="sticky left-0 bg-background border-b border-border px-3 py-3">Artist</th>{data.months.map(m=><th className="border-b border-border px-3 py-3" key={m}>{m==='2026-09'?'Sep · in progress':m}</th>)}<th className="border-b border-border px-3 py-3">Trend</th></tr></thead><tbody>{data.artists.filter(a=>a.name.toLowerCase().includes(search.toLowerCase())).map(a=><tr key={a.name}><td className="sticky left-0 bg-background border-b border-border px-3 py-4 font-semibold">{a.name}</td>{data.months.map((m,i)=><td key={m} className="border-b border-border px-3 py-4">{display(a,i)}</td>)}<td className="border-b border-border px-3 py-2"><Sparkline artist={a}/></td></tr>)}</tbody></table></div></PageFrame>}
