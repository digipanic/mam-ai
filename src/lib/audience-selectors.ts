import type { ArtistRecord, AudienceMonitorData, MonthlyPoint } from "@/lib/audience-types";

export const slugifyArtist = (name: string) => encodeURIComponent(name.toLocaleLowerCase());
export const findArtist = (artists: ArtistRecord[], slug: string) => artists.find((artist) => slugifyArtist(artist.name) === slug);
export const formatNumber = (value: number | null | undefined) => value == null ? "—" : new Intl.NumberFormat("en-US").format(value);
export const formatSigned = (value: number | null | undefined) => value == null ? "—" : `${value > 0 ? "+" : ""}${formatNumber(value)}`;
export const formatPercent = (value: number | null | undefined) => value == null ? "—" : `${value > 0 ? "+" : ""}${value.toFixed(2)}%`;
export const formatDate = (value: string | null | undefined, time = false) => { if (!value) return "—"; const date = new Date(value); return Number.isNaN(date.valueOf()) ? value : date.toLocaleDateString("en-GB", { day: "numeric", month: "short", year: "numeric", ...(time ? { hour: "2-digit", minute: "2-digit" } : {}) }); };
export type HistoricalPerformance = { start: { date: string; followers: number }; end: { date: string; followers: number }; change: number; growthPercent: number };
export const historicalPerformance = (history: ArtistRecord["historical"]["instagram"]): HistoricalPerformance | null => { const start = history[0]; const end = history.at(-1); if (!start || !end || start.date === end.date || start.followers === 0) return null; return { start, end, change: end.followers - start.followers, growthPercent: ((end.followers - start.followers) / start.followers) * 100 }; };
export const rank = (artists: ArtistRecord[], value: (artist: ArtistRecord) => number | null) => [...artists].filter((artist) => value(artist) !== null).sort((a,b) => (value(b) ?? -Infinity) - (value(a) ?? -Infinity));
export const median = (values: (number | null)[]) => { const list = values.filter((value): value is number => value !== null).sort((a,b) => a-b); if (!list.length) return null; const half = Math.floor(list.length / 2); const center = list[half] ?? 0; return list.length % 2 ? center : ((list[half - 1] ?? 0) + center) / 2; };
export const trajectory = (history: { followers: number | null }[]) => { const valid = history.filter((point): point is { followers: number } => point.followers !== null); const firstPoint = valid[0]; if (valid.length < 2 || !firstPoint) return "Insufficient history"; const first = firstPoint.followers, last = valid.at(-1)?.followers ?? first; const rate = (last - first) / first; if (rate > .1) return "Strong growth"; if (rate > .02) return "Growing"; if (rate < -.02) return "Declining"; return "Stable"; };
export type MonthlyPerformance = { artist: ArtistRecord; start: { month: string; followers: number }; end: { month: string; followers: number }; change: number; growthPercent: number; momentum: "Accelerating" | "Decelerating" | "Stable" | null };
type ValidMonthlyPoint = MonthlyPoint & { followers: number };
const validMonthlyPoints = (artist: ArtistRecord): ValidMonthlyPoint[] => artist.monthlyHistory.filter((point): point is ValidMonthlyPoint => point.followers !== null);
export const monthlyPerformance = (artist: ArtistRecord): MonthlyPerformance | null => { const history = validMonthlyPoints(artist); const start = history[0]; const end = history.at(-1); if (!start || !end || start.month === end.month || start.followers === 0) return null; const recent = history.slice(-3); const older = recent[0]; const middle = recent[1]; const newest = recent[2]; const previousGain = older && middle ? middle.followers - older.followers : null; const latestGain = middle && newest ? newest.followers - middle.followers : null; const momentum = previousGain === null || latestGain === null ? null : Math.abs(latestGain - previousGain) <= Math.max(10, Math.abs(previousGain) * 0.15) ? "Stable" : latestGain > previousGain ? "Accelerating" : "Decelerating"; return { artist, start, end, change: end.followers - start.followers, growthPercent: ((end.followers - start.followers) / start.followers) * 100, momentum }; };
export const monthlyRosterSeries = (data: AudienceMonitorData) => data.months.map((month) => { const values = data.artists.map((artist) => artist.monthlyHistory.find((point) => point.month === month)?.followers).filter((value): value is number => value !== null && value !== undefined); return { month, audience: values.length ? values.reduce((total, value) => total + value, 0) : null, coverage: values.length }; });
export type MonthlyPace = { month: string; change: number | null; medianChange: number | null; coverage: number };
export const monthlyPace = (data: AudienceMonitorData): MonthlyPace[] => data.months.map((month, index) => { const previousMonth = data.months[index - 1]; if (!previousMonth) return { month, change: null, medianChange: null, coverage: 0 }; const changes = data.artists.map((artist) => { const current = artist.monthlyHistory.find((point) => point.month === month)?.followers; const previous = artist.monthlyHistory.find((point) => point.month === previousMonth)?.followers; return current !== null && current !== undefined && previous !== null && previous !== undefined ? current - previous : null; }); const valid = changes.filter((change): change is number => change !== null); return { month, change: valid.length ? valid.reduce((total, change) => total + change, 0) : null, medianChange: median(changes), coverage: valid.length }; });
export const monthlyConsistency = (artist: ArtistRecord) => { const points = validMonthlyPoints(artist); const changes = points.slice(1).map((point, index) => point.followers - (points[index]?.followers ?? point.followers)); if (changes.length < 2) return "Limited history"; return changes.every((change) => change > 0) ? "Consistently growing" : "Mixed movement"; };
export const recordHigh = (artist: ArtistRecord) => { const points = validMonthlyPoints(artist); const latest = points.at(-1); return Boolean(latest && points.slice(0, -1).every((point) => latest.followers > point.followers)); };
export const percentile = (value: number | null, values: (number | null)[]) => { if (value === null) return null; const valid = values.filter((item): item is number => item !== null).sort((a, b) => a - b); if (!valid.length) return null; return (valid.filter((item) => item <= value).length / valid.length) * 100; };
export const platformCoverage = (data: AudienceMonitorData) => ({ instagram: data.artists.filter((artist) => artist.instagram.audience !== null).length, comparableInstagram: data.artists.filter((artist) => artist.instagram.change !== null).length, soundcloud: data.artists.filter((artist) => artist.soundcloud.audience !== null).length, residentAdvisor: data.artists.filter((artist) => artist.residentAdvisor.audience !== null).length, monthlyHistory: data.artists.filter((artist) => validMonthlyPoints(artist).length > 0).length });
export const dashboardSummary = (data: AudienceMonitorData) => {
 const comparable = data.artists.filter(a => a.instagram.change !== null && a.instagram.growthPercent !== null);
 const currentRank = rank(data.artists, a => a.instagram.audience), growthRank = rank(data.artists, a => a.instagram.growthPercent), gainRank = rank(data.artists, a => a.instagram.change);
 const monthly = data.artists.map(monthlyPerformance).filter((item): item is MonthlyPerformance => item !== null); const monthlyGainRank = [...monthly].sort((a,b) => b.change - a.change); const monthlyGrowthRank = [...monthly].sort((a,b) => b.growthPercent - a.growthPercent); const accelerating = monthly.filter((item) => item.momentum === "Accelerating");
 return { comparable, leaders: { audience: currentRank[0], growth: growthRank[0], gain: gainRank[0], monthlyGain: monthlyGainRank[0], monthlyGrowth: monthlyGrowthRank[0] }, currentRank, growthRank, gainRank, growing: comparable.filter(a=> (a.instagram.change ?? 0)>0), declining: comparable.filter(a=> (a.instagram.change ?? 0)<=0), monthly, monthlyGainRank, monthlyGrowthRank, accelerating, medians: { audience: median(data.artists.map(a=>a.instagram.audience)), change: median(comparable.map(a=>a.instagram.change)), growth: median(comparable.map(a=>a.instagram.growthPercent)) }, coverage: platformCoverage(data) };
};
