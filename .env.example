# Server-side only — never prefix with VITE_, or these would ship to the browser.
# From the Google Cloud service account's downloaded JSON key file.
GOOGLE_SERVICE_ACCOUNT_EMAIL=
# Paste the "private_key" field's value as one line, keeping the literal \n
# sequences — do not convert them to real newlines.
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY=
