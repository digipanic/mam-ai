# Server-side only — never prefix with VITE_, or it would ship to the browser.
# A Google API key with the Sheets API enabled, used to read the Minor AM
# monitoring sheet. The sheet must be shared as "Anyone with the link can view".
GOOGLE_SHEETS_API_KEY=
